name: "IB210370"
description: Scratch & Collect.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_svg: ^2.0.7
  dio: ^5.2.1+1
  json_annotation: ^4.5.0
  flutter_secure_storage: ^9.0.0
  skeletons: ^0.0.3
  flutter_rating_bar: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - lib/modules/shared/assets/
    - lib/modules/shared/assets/icons/
